import React from "react";
import { firebase } from "../components/Firebase/Firebase";

function Auth() {
  return (
    <div>
      this was supposed to be auth section but it is in process of building,
      have some patience and coordinate.
    </div>
  );
}
